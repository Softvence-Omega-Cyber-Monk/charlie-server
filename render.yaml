services:
  - type: web
    name: tda-academy-backend
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm build && npx prisma generate
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        scope: project
